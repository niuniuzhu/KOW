import { VEntityAction } from "./VEntityAction";
export class VActShake extends VEntityAction {
    OnInit(def) {
        super.OnInit(def);
    }
    OnEnter(param) {
        super.OnEnter(param);
    }
}
