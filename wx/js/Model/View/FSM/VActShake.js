import { VEntityStateAction } from "./VEntityStateAction";
export class VActShake extends VEntityStateAction {
    OnInit(def) {
        super.OnInit(def);
    }
    OnEnter(param) {
        super.OnEnter(param);
    }
}
